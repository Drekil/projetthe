<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('main')}}">Thé</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Thés et infusions
                    </a>
                    <ul class="dropdown-menu"> 
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "the-vert"}) }}"> Thés verts </a></li>
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "the-noir"}) }}"> Thés noirs </a></li>
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "infusions"}) }}"> Infusions </a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Epicerie fine
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "miels"}) }}"> Miels </a></li>
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "confitures-et-tartinables"}) }}"> Confitures et tartinables </a></li>
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "biscuits"}) }}"> Biscuits </a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Accessoire
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "theiere"}) }}"> Théières </a></li>
                        <li><a class="dropdown-item" href="{{ path('categories_list', {slug: "tasses"}) }}"> Tasses </a></li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('cart_index') }}">Mon panier</a>
                    </li>
                {% if app.user %}
                {% if is_granted('ROLE_PRODUCT_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('admin_index') }}">Administration</a>
                    </li>
                {% endif %}
                            
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('user_edit', {id : app.user.id }) }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href=" {{ path('app_logout') }}">Me déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href=" {{ path('app_login') }}">Me connecter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href=" {{ path('app_register') }}">M'inscrire</a>
                    </li>
                {% endif %}
            
            </ul>
        </div>
    </div>
</nav>